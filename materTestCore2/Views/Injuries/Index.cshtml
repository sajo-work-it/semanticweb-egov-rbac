@model IEnumerable<materTestCore2.Models.EGovModels.health.Injury>

@{
    Layout = "~/Views/Shared/health_Layout.cshtml";

    ViewData["Title"] = "Index";
}

<h1>أنواع الإصابات</h1>

<p>
@*    <a asp-action="Create">إضافة</a>
*@</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.injury_name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.injury_name)
            </td>
@*            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>*@
        </tr>
}
    </tbody>
</table>
